# Manual dry-run only (workflow_dispatch). Never runs on push to main.
# Same validation as Release preflight; no tag or GitHub Release created. See docs/release.md
name: Release check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preflight:
    uses: ./.github/workflows/preflight-release.yml
