# Dry-run the release pipeline (preflight only — no tag or GitHub Release created).
# Version is read from Cargo.toml on main. See docs/release.md
name: Release check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preflight:
    uses: ./.github/workflows/preflight-release.yml
